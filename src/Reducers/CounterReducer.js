/*
import {
    INCREMENT,
    DECREMENT
} from '../Constants';

const initialState = {
    count: 0,
    isIncrementing: null,
    isDecrementing: null
};

export default (state = initialState, action) => {

    switch(action.type) {
        case INCREMENT:
            return {
                ...state,
                count: state.count + 1,
                isIncrementing: true
            };
        case DECREMENT:
            return {
                ...state,
                count: state.count - 1,
                isDecrementing: true
            };
        default:
            return state;
    }

}
*/